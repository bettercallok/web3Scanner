# Django Core
Django==4.2.13
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.3.1
django-ratelimit==4.1.0
django-environ==0.11.2
django-celery-beat==2.6.0
django-celery-results==2.5.1
psycopg2-binary==2.9.9

# Async / WebSocket
channels==4.1.0
channels-redis==4.2.0
daphne==4.1.2
websockets==12.0

# Celery
celery==5.3.6
redis==5.0.4

# Analysis Tools (Slither only — Mythril is installed in a separate venv in Dockerfile; the two
# tools have incompatible eth-hash/web3 stacks but both are invoked as subprocess CLIs.)
slither-analyzer==0.10.3

# AI / RAG
langchain==0.2.6
langchain-community==0.2.6
langchain-chroma==0.1.2
chromadb==0.5.0

# HTTP clients
requests==2.32.3
httpx==0.27.0

# PDF generation
weasyprint==61.2
pydyf>=0.8.0,<0.11.0
jinja2==3.1.4

# Utilities
python-dotenv==1.0.1
pydantic==2.7.4
